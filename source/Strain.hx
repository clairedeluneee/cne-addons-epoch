package source;

public class Strain {
    public static function getStrainOfStrumline(strumline:Dynamic):Float {
        var individualStrains = new Array(strumLine.members.length);

        trace(individualStrains);
    }
}
